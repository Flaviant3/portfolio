{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block stylesheets %}
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            color: #ffffff;
            background: linear-gradient(135deg, #1E1E2E, #252542);
            text-align: center;
            padding: 20px;
        }

        .hero {
            margin-top: 80px;
            padding: 60px 20px;
            background: rgba(0, 0, 0, 0.5);
            border-radius: 15px;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.3);
        }

        .hero h1 {
            font-size: 3em;
            color: #ffcc00;
            margin-bottom: 10px;
        }

        .hero p {
            font-size: 1.2em;
            opacity: 0.8;
        }

        .about, .projects, .contact, .timeline, .stack, .feedback, .fun, .testimonials {
            margin-top: 50px;
            padding: 40px 20px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.3);
        }

        .about h2, .projects h2, .contact h2, .timeline h2, .stack h2, .feedback h2, .fun h2, .testimonials h2 {
            color: #ffcc00;
            font-size: 2em;
            margin-bottom: 15px;
        }

        .projects-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
            margin-top: 20px;
        }

        .project-card {
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 10px;
            width: 300px;
            text-align: center;
            transition: transform 0.3s ease-in-out;
        }

        .project-card img {
            max-width: 100%;
            border-radius: 10px;
        }

        .project-card:hover {
            transform: scale(1.05);
        }

        .btn {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            font-size: 1.1em;
            font-weight: bold;
            background: #ffcc00;
            color: #1E1E2E;
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.3s ease-in-out;
        }

        .btn:hover {
            background: #ffaa00;
            transform: scale(1.05);
        }

        .timeline ul {
            list-style: none;
            padding: 0;
        }

        .timeline li {
            margin: 10px 0;
        }

        .stack-icons {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
            font-size: 2em;
        }

        .feedback form {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
        }

        .feedback textarea {
            width: 80%;
            height: 100px;
            border-radius: 8px;
            padding: 10px;
        }

        .testimonials blockquote {
            font-style: italic;
            opacity: 0.9;
            margin-bottom: 10px;
        }
    </style>
{% endblock %}

{% block body %}
    <section class="hero">
        <h1>Bienvenue sur mon Portfolio</h1>
        <p>Développeur passionné, créatif et toujours en quête d'innovation.</p>
        <a href="{{ path('projects') }}" class="btn">Voir mes Projets</a>
    </section>

    <section class="about">
        <h2>À propos de moi</h2>
        <p>Je suis un développeur full-stack avec une expertise en <strong>Python, JavaScript et Symfony</strong>. J'adore créer des applications performantes et intuitives.</p>
    </section>

    <section class="projects">
        <h2>Mes Projets</h2>
        <div class="projects-container">
            {% for project in projects %}
                <div class="project-card">
                    {% if project.image %}
                        <img src="{{ project.image }}" alt="{{ project.title }}">
                    {% else %}
                        <img src="https://via.placeholder.com/300x200.png?text=Aucune+Image" alt="Pas d'image">
                    {% endif %}
                    <h3>{{ project.title }}</h3>
                    <p>{{ project.description }}</p>
                    <a href="{{ path('show_project', { slug: project.slug }) }}" class="btn">Découvrir</a>
                </div>
            {% endfor %}
        </div>
    </section>

    <section class="timeline">
        <h2>Mon Parcours</h2>
        <ul>
            <li>2020 - BTS SIO</li>
            <li>2022 - Stage développeur Symfony</li>
            <li>2023 - Développement de projets fullstack</li>
        </ul>
    </section>

    <section class="stack">
        <h2>Stack Technique</h2>
        <div class="stack-icons">
            <span>🐍 Python</span>
            <span>⚙️ Symfony</span>
            <span>⚛️ React</span>
            <span>🌐 HTML/CSS/JS</span>
            <span>🛢️ MySQL</span>
        </div>
    </section>

    <section class="testimonials">
        <h2>Témoignages</h2>
        <blockquote>"Travail sérieux et toujours à l’écoute des besoins." - Professeur M.</blockquote>
        <blockquote>"Un vrai sens du détail et de la qualité." - Client X.</blockquote>
    </section>

    <section class="fun">
        <h2>Fun Zone 🎮</h2>
        <p>Un mini-jeu ou animation à venir... 👀</p>
    </section>

    <section class="contact">
        <h2>Me Contacter</h2>
        <p>Envie de collaborer ou d’échanger ? N'hésite pas à me contacter !</p>
        <a href="{{ path('contact') }}" class="btn">Contactez-moi</a>
    </section>
{% endblock %}
